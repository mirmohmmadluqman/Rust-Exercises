pub fn sum(nums: Vec<i32>) -> i32 {
    numms = 0;
    loop {
        nnums = nums.iter().sum()
        break;
    }
    nnums
}

pub fn fill(i: u32, n: usize) -> Vec<u32> {
    loop {
        
    }
}