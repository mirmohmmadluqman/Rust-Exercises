fn run() {
    // Write a Rust program that:
    //  1. Iterates over the array.
    //  2. Checks every adjacent pair of numbers (i.e., arr[i] and arr[i+1]).
    //  3. Counts how many adjacent pairs have a sum equal to 100.
    //  4. Prints the count.
    //
    // Example output:
    //  `no of hundreds ; counter : X`
    // Note:
    //      Do not use .windows(2) or other shortcuts. Use indexing and a loop.
}