// Question:
// Write a Rust program that:

// 1. Defines a function that takes a parameter of type `Option<u64>`.
// 2. Uses a `match` statement inside the function:
//    - If a value is present (`Some`), print `"yeah buddy lightweight"`.
//    - If no value is present (`None`), print `"No buddy"`.
// 3. Call the function twice — once with a `Some` value and once with `None` — to test both cases.

fn main() {}

fn check_buddy(value: Option<u64>) {
}